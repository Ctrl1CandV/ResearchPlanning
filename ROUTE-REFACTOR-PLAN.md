# 展示层重构方案：把「路线」提到台前（v2 · 2026-08-30）

> 状态：v2（已按设计评审 required_fixes 修订；v1 的 4.5「翻译层/pitch 移入折叠」已撤回，
> 4.6 改为档案层专用类）。本文档是实施依据；实施前后各有一次记忆干净的 subagent 审查，
> 放行后才进入下一阶段。与 REDESIGN-PLAN.md（v3）的关系：不推翻其任何既有决策，
> 只在其基础上叠加「路线层」。

---

## 0. 约束的解释（先划边界）

- **内容不动**：`site/data/*.js` 的文案、结论、证据标注零改动。新增的可见文字只允许两类：
  ①从现有数据**派生**的导航标签（如路线图节点的「下一站」徽章、周条的「本周」）；
  ②结构性小标签（如顶栏 chip 的「当前阶段」）。
- **设计语言不动**：配色令牌（蓝图靛 `#7d9bff`、证据三色纪律）、字体、组件语汇（card/badge/callout/
  details/timeline）、暗浅双主题、图纸底纹等视觉身份全部保留。新组件只用现有令牌拼装。
- **可以大改的**：信息架构——区块顺序、默认展开状态、导航组件、当前状态的视觉表达。

## 1. 现状诊断（2026-08-30 浏览器实测，截图存 `_shots/before-*.png`）

| 页面 | 实测结论 |
|---|---|
| dashboard | 「当前阶段」卡是好的；其后即被 4 张超重纠偏卡（3 列大文本墙）占据中屏；三年路线图折叠在页底几乎不可见。「我在一条三年路线的哪里」没有可视化表达 |
| reading | 13 篇公共必读 2 列长墙占满首屏之后才是方向卡；真正按周推进的 90 天计划折叠在页面最底部，且无「本周」标记——学习路线本身是隐藏的 |
| research | 六个切口大卡视觉权重完全相同，推荐的 A 只有一枚小徽章差异；「给导师的翻译层」表格 + pitch 引用块（档案材料）展开暴露在页中，与决策内容抢注意力 |
| jobs | 地域三卡与四级阶梯结构尚好，但阶梯无「当前在哪一步」高亮 |
| 全局 | 顶栏右侧是「进度保存在本机」（侧栏页脚已有同样文字，冗余）；折叠档案块与执行区块折叠样式同权，扫视时无法区分「要做的事」与「参考资料」 |

一句话：**内容是完整的档案库，但缺一层「路线」——读者自己从 40+ 个区块里拼装顺序。**

## 2. 调研输入（2026-08-30）

1. **roadmap.sh / kamranahmedse·developer-roadmap（GitHub ~300k★）**：路线图本身是页面主角，
   主题为可点击节点、节点背后挂资源、可标记 done/in-progress/skip 追踪进度；内容层为
   Markdown（解释）+ JSON 图数据（结构）。社区批评同样明确：节点过多的图「让人不知所措」。
   → 借鉴：**路线要作为一等对象永远可见、可点击、可标记进度**；不借：节点图
   （本站是单人线性三年路线，图结构是过度设计）。
2. **The Odin Project · Paths**：显式线性叙事——"This is where it all begins" → "Then choose a
   learning path" → "courses should be taken in the order displayed"；层级 = 身份 → 概览 → 行动 CTA。
   → 借鉴：**用顺序声明 + 每阶段一张卡 + 明确的下一步**，而不是让读者猜顺序。
3. **NN/g · Progressive Disclosure**：常用/必用内容前置，高级/罕用内容收纳到二级交互；
   警告：不可随意隐藏而伤害可发现性 → 折叠标题必须自带信息量（本站已有此纪律，继续强化）。
4. **本站已有资产**：`currentPhase/PHASES`（阶段感知）、`plan90Week`（当前周计算）、
   `renderTrackCard` 的 stage-flow stepper、`JOBS.timeline`（11 条三年节点）、
   `state.checks`（localStorage 进度）。**所需数据全部已在，纯渲染层工作。**

## 3. 设计模型：三层信息架构

- **L1 路线层（新增，视觉最前）**：跨时间的「路径」对象。特征：永远展开、占据首屏焦点、
  有「当前位置」标记。三处：仪表盘三年路线图 / 阅读页 12 周条（chip 可点击跳周）/
  岗位页阶梯当前步。路线图节点本身不做链接（点击目标与 timeline 条目无可靠映射，不硬造）。
- **L2 行动层（已有，提权）**：当前阶段行动清单、方向卡、checklist——默认展开，样式不变。
- **L3 档案层（已有，降噪）**：纠偏、翻译层、饱和、竞争、指标、口径——保持折叠，
  且折叠样式与执行区块在视觉上明显区分（更小更暗的标题）。

## 4. 改动清单（按页面）

### 4.1 dashboard · 三年主路线图（新组件 `renderRoadmapSpine(now)`）
- 数据源：`JOBS.timeline` 全部 11 条（when/title/detail/milestone/type 现成）+ 末端
  「毕业」终止节点。毕业节点用渲染层常量（`GRADUATE_ISO = '2029-06-30'`，注释「每年复核」，
  与 `ENROLL_ISO` 同址同惯例），不解析 `DATA.meta.graduation` 的句子。
- 节点状态由日期派生：`when` 解析起始月（`/^(\d{4})\.(\d{2})/`，已核对 timeline 全部 11 条均命中；
  解析失败兜底为 future）→ 已过（灰）/ 当前进行中（强调）或下一站（强调 + 「下一站」徽章；
  今天 2026-08-30 处于入学前，「入学」节点即下一站）/ 未来（空心暗点）。
  `forecast` 节点保留现有预测徽章。节点不可点击。
- 视觉：垂直 spine（左侧连线 + 圆点 + 日期 + 标题 + 一行 detail）；只复用现有 badge/timeline 语汇。
- 位置：hero 主线卡 → 当前阶段卡 → **三年路线图** → 进度总览 → 4 统计 → 当前研究决策 →
  4 条纠偏（c3→c2 降密度）→ 边界卡。原「三年阶段图」折叠块移除（其 slice(0,8) 本就丢弃了
  2028.07 之后的 3 个节点；spine 渲染全量 11 条 + 毕业节点属净增益，jobs 页完整时间线保留不动）。

### 4.2 全局 · 顶栏当前位置 chip
- `index.html` topbar 右侧「进度保存在本机」替换为 `#phase-chip`（侧栏页脚已保留同义文案）。
- `app.js` 启动与每次渲染时填充：`当前阶段 · {phase.name}`（+ 开学后附 `W{n}` 徽章）。
  复用 `currentPhase()/plan90Week()`，不新增状态。

### 4.3 reading · 12 周周条（新组件 `renderWeekStrip()`）
- W1—W12 chip，按 Phase 1/2/3 分组；「本周」高亮（`plan90Week` 已有；入学前（0）与
  过执行期（13）均无本周高亮）；已勾周（`state.checks` 的 `week-Wx`）显示完成态；
  里程碑周 W4/W8/W12 星标。
- 点击 chip：展开对应 Phase 的折叠块并滚动到该周条目（`plan90Item` 的 article 加 `id="week-item-Wn"`）。
- 三个 Phase 折叠块的默认展开项：入学前 → Phase 1；开学后 → 当前周所在 Phase；
  过执行期 → Phase 3。
- 位置：「90 天启动路线」标题与换轨 callout 之后、Phase 折叠块之前。公共必读区不动。

### 4.4 jobs · 阶梯当前步高亮
- `currentPhase().id` 映射当前 STEP（pre→0，y1a/y1b→1，y2→2，sprint/intern→3），
  对应阶梯条目加 `current` 类（左侧强调边 + 「当前阶段」徽章）。数据零改动。
- 映射语义注释写进代码：pre 阶段 STEP 0 标注为「下一站」语义（入学首月即将开始）；
  y2（2027.09 起）标 STEP 2 是提前半年的准备窗口（STEP 2 窗口为 2028.01—02，
  提前亮出与 timeline 的「2027.10—11 接触 mentor」一致）。

### 4.5 research · 不动区块位置（v2 撤回 v1 的移动方案）
- 翻译层表格与 pitch 保留在原位展开——这是 REDESIGN-PLAN.md 阶段 2 的既有决策
  （「双轨、翻译层、pitch 保留展开（组会要用）」），且 `RESEARCH.summary` 第 5 条明写
  「组会开场用本页底部 pitch 的说法」；在 data 零改动约束下移动会让要点与正文漂移，
  违反 MAINTENANCE 第 3 节纪律。研究页的「降噪」改由 4.6 的档案层专用类承担
  （该页已折叠的档案块变安静，执行区块不变）。

### 4.6 档案层降噪（v2：作用域收窄为显式清单）
- `details()` 增加可选标记参数，仅下列**档案类**折叠块渲染为 `acc ref`（summary 字号/颜色
  弱化一档，保持对比度 ≥4.5:1；展开内容样式不变）：
  - baseline：可关注实验室、就业事实与不可用口径、WAF 说明
  - research：已拒绝的方向、指标体系、9 条饱和判断、竞争团队雷达、见导师材料
  - jobs：各城市策略明细、城市记录分布与可投池、抽样与字段质量、数据边界
  - skills：市场信号矩阵、面试四条能力线
- **明确不加** `ref` 的执行层折叠（保持现状视觉）：reading 的公共必读进度与 Phase 1—3、
  六切口卡内的「方法草案」、jobs 页其余折叠。CSS 只写 `.acc.ref > summary`，无全局选择器。

## 5. 明确不做

- 不做 roadmap.sh 式节点图（单人线性路线用图是过度设计，且调研显示节点图易过载）
- 不改任何 `data/*.js` 文案、不改配色/字体/令牌、不动导航 10 项与路由、不加依赖、不做后端
- 不动 `now-card` 的类名与徽章文本（`scripts/phase-smoke.js` 断言依赖）
- 不改 id 命名规则（持久化勾选依赖）

## 6. 验收标准

1. 打开 dashboard：3 秒内读到「当前阶段 + 三年路线全景 + 下一站」；路线图永远可见（不折叠）。
2. 打开 reading：能看到 12 周全景条与「本周」位置；点击任一周直达该周条目。
3. 打开 jobs：四级阶梯能一眼看出当前在哪一步。
4. 打开顶栏任意页：chip 显示「当前阶段 · {阶段名}」（开学后附 W{n} 徽章）；入学前与过执行期不显示周徽章。
5. 档案层折叠块（4.6 清单）summary 视觉弱于执行区块；执行层折叠块样式与现状一致。
6. `node --check` 全绿；`node scripts/render-smoke.js` 十路由 PASS；`node scripts/phase-smoke.js` PASS；
   `npm test` 相对实施前不新增失败（既有 17 个旧政策断言失败与本次无关）。
7. 暗浅双主题、窄窗（<1000px）下新组件不破版。
8. MAINTENANCE.md 组件清单与新类约定同步更新（smoke 命令路径已在清理阶段更新为 scripts/ 下）。

## 7. 风险与回退

| 风险 | 缓解 |
|---|---|
| spine 日期解析出错导致全 future | 解析函数带兜底（失败即 future），并有 render 冒烟兜底 |
| render.test.js 断言区块顺序 | 实施前先读测试；断言若绑定旧行为，按新行为更新断言并注明原因 |
| 档案降噪让折叠块太不显眼 | 仅弱化一档（保持 4.5:1 对比度下限），页内目录/搜索仍可达 |
| 单文件体积膨胀 | 新组件合计预计 <120 行 JS + <150 行 CSS，复用现有类 |

回退：改动集中在 `app.js`（4 个函数 + 渲染器内两处顺序）、`style.css`（新增类）、
`index.html`（1 个 span），任一可单独 git 回滚。
