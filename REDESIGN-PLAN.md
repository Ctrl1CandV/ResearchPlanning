# 决策工作台 · 展示与内容重构方案（v3 修订稿）

> 状态：v2 修订（已按第一轮审查意见逐条修正）。本文档是实施依据。
> 日期：2026-08-29。针对的两个问题来自网站所有者（研一新生）的直接反馈。
> 修订说明：v1 基于 2026-08-28 之前的旧结构撰写，第一轮审查发现站点的数据与渲染层
> 已被另一轮修订重构（阅读页改为「公共必读+方向卡」、工具页缩编为速查卡、求职资产降级、
> 北京改中转、主线转向假成功检测）。v2 全部按**当前真实结构**重新撰写。

---

## 0. 现状基线（实施前必读，均已逐项核实）

数据与渲染层的当前事实（2026-08-29 grep + 运行核对）：

| 页面 | 当前结构 | 已达标的部分 |
|---|---|---|
| dashboard | 宣言卡 + 4 统计 + 4 纠偏 + 研究决策 + 入学清单 + 边界 + 三年阶段图 | — |
| baseline | 导师画像(10 论文/7 项目/盲区) + 制度 8 问(details) + 校企 5 项 + 实验室 + 就业 + WAF | 制度已是折叠 |
| research | 定位 + 6 切口大卡 + rejected(已折叠) + 双轨 + 指标 7 组 + 饱和 + 竞争 + 翻译层 + pitch | rejected 已折叠 |
| reading | 使用说明 + common 13 篇恒显 + 进度清单 + 方向 chips(A—F 单卡) + 90 天 12 周 + fallback | 方向已是单卡切换 |
| tools | 声明 + core 11 行速查表 + companion 指引 | **整页已达标，不动** |
| jobs | pageNote + 城市策略(含北京中转卡) + 岗位族 9 + 岗位卡 14 + 阶梯 4 + 时间线 + 口径(已折叠 2 处) + 风险 | 口径已折叠 |
| skills | 口径声明 + 信号矩阵 19 + 学习路线 13(含学习参考) + 面试 4 线 | — |
| portfolio | 原则 + 3 项目(Atlas/AgentParliament/代码图谱) + 错误故事 | **已达标，不动** |
| career | 已自我降级为「方向参考，2027.12 重做」 | **已达标，不动** |
| verify | 12 核验项(数据顺序) + 每月巡检 | — |

另有三处**现状缺陷**（一并修正）：

1. `app.js:240` dashboard 行动清单写「完成 L0 前四篇 + L4 假成功三篇」——L0/L4 结构已废，属陈旧文案。
2. `RESEARCH.firstMail / reportDeck / reportTips` 仍在 data 中但**渲染器未引用**，页面不可见。
3. 进度勾选刷新即失（`state.checks` 仅内存），工作台属性不成立。

---

## 1. 问题定义（用户原话的落点）

**问题 1：内容太多太杂、堆在一起，看不清重点。**
当前每页仍把「结论、执行内容、情报/证据」平铺展开。典型：research 页 6 张大卡后接
指标 7 组 + 饱和表 + 竞争雷达 + 翻译层连排；baseline 页就业/实验室等背景档案与导师画像同权重。

**问题 2：信息与「研一新生」不匹配。**
无用（对当前）：情报类内容（饱和分析、竞争雷达、信号矩阵、快照统计）与执行内容同权重展示。
不足：无时间感知（今天距入学几天、当前阶段该做什么）、dashboard 行动清单陈旧、进度不持久。

衡量目标（验收逐项检查）：
- 每页首屏（约 1000px）内读到本页 3—5 条要点；
- 默认展开内容以执行类为主，情报类默认折叠、一键可达（不删除）；
- 打开 dashboard 30 秒内能回答「我现在该做什么」。

---

## 2. 设计原则

1. **分层不删减**：信息不丢，只改默认可见性。看全（可展开）与抓重点（默认见要点）同时成立。
2. **研一视角**：默认可见性由「当前阶段是否用得上」决定；但**不做页级远期标签**——
   jobs 的实习阶梯、skills 的学习路线就是研一内容，整页降级会误伤（第一轮审查意见 #3，已采纳）。
3. **证据纪律不动**：一手/二手/推断、claim/evidence 徽章、口径说明全部保留，只是默认折叠。
4. **不过度设计**：不换栈、不加依赖、不增页面、不动导航 10 项；组件只加一个（要点块），
   机制只加一个（折叠约定）。已达标页面（tools / career / portfolio）明确不动。
5. **忠于现状**：要点一律从该页**当前**文案提炼，不引用已废结构，不新造结论。

---

## 3. 阶段方案（四阶段，各自独立交付、独立回滚）

### 阶段 1 · 要点层 + 陈旧文案修正

1. `app.js` 新增 `pageSummary(items)` 组件：标题「本页要点」+ 3—5 条，置于 `pageHead` 之后。
   样式介于 hero 与普通卡片之间（左色条、字号略大），一页最多一个。
2. 各 data 模块加 `summary` 字段（3—5 条/页，从该页**当前**句子提炼）。tools / career / portfolio 不加。
3. 各渲染器在 `pageHead` 后插入（无 `summary` 字段的页自动跳过）。
4. **修正 `app.js:240` 陈旧文案**：行动清单改为引用现行结构，如
   「公共必读前 4 篇 + 方向 A 必读 3 篇，跑通 Atlas 全量测试」（实施时按 reading 现状定稿）。
5. 全站 grep 审计其他已废结构引用（L0/L1/L2/L3/L4 字样），一并清理。

验收：任意页面首屏见要点；要点与正文逐条能对上；无已废结构字样残留。

### 阶段 2 · 证据层折叠（逐页，复用现有 details.acc）

| 页面 | 动作 |
|---|---|
| research | 「已经饱和的方向」「竞争团队雷达」→ 折叠（情报类）；「指标体系」8 组 → 折叠（实验设计时查阅）；双轨、翻译层、pitch 保留展开（组会要用） |
| baseline | 「可关注实验室」「就业事实与不可用口径」+ 底部 WAF 说明 → 折叠（背景档案）；导师画像、方法签名、校企 5 项保留展开 |
| reading | 「公共必读进度」勾选清单 → 折叠；90 天计划按 Phase 分 3 组折叠，默认展开 Phase 1（12 周时间线是本页最长的墙）；方向卡、fallback 不动 |
| jobs | 「不要跨过的数据边界」→ 折叠；**section 顺序调整**：实习阶梯上移到岗位样本之前（研一的核心是阶梯，岗位样本是远期目标地图）；其余不动（口径 2 处已折叠） |
| skills | 「市场信号矩阵」「面试四条能力线」→ 折叠（证据/2028）；学习路线 13 项含学习参考保留展开（执行） |
| verify | 渲染按 impact 排序（critical → high → medium），数据不动 |

**jobs 不做默认阶段筛选**：含 `masterYear1` 的岗位仅 2 条，默认筛选会让页面近乎空白
（第一轮审查意见 #6，已采纳）。改为上述顺序调整。

验收：折叠标题自带信息量（名称+条数）；展开后内容完整；搜索仍命中折叠内文本（现状机制不变）。

### 阶段 3 · dashboard 驾驶舱（阶段感知 + 本阶段行动）

自上而下：宣言卡 → **阶段感知卡** → **本阶段行动清单** → 4 统计 → 4 纠偏 → 研究决策 → 边界 → 三年阶段图(折叠)。

1. **阶段感知卡**（新）：`Date.now()` 对常量里程碑（入学 2026-09-01 / 研一寒假 2027-01 /
   研二 2027-09 / 暑期实习主投 2028-02）计算当前阶段，显示阶段名 + 距下一里程碑天数 + 一句话定位。
   里程碑集中在渲染器顶部常量，注释「每年复核」。
2. **本阶段行动清单**（修正并聚合，替换现有 immediate）：
   入学前 5 条——发/确认导师邮件进度、公共必读前 4 篇、两项学位制度核验（链 verify 页）、
   算力确认（链 verify 页）、Atlas 全量测试；入学后自动切换为 90 天当前周 + 当月核验。
   每条带锚点链接到对应页面。
3. **三年阶段图 → 折叠**（远期信息）。4 统计块压缩保留。

**本阶段不含进度条**（见阶段 4 的绑定说明，第一轮审查意见 #5，已采纳）。

验收：模拟 3 个日期（2026-08-29 / 2026-10 / 2028-02）渲染正确；30 秒测试通过。

### 阶段 4 · 进度持久化 + 进度总览（绑定实施）

1. `localStorage` 持久化：`state.checks`（稳定 id，规则不变）+ 主题 + 当前方向 track。
   IIFE 启动时 try/catch 读取，写回在 change/click 回调里；异常静默降级为会话态（file:// 限制已知情）。
   侧栏「状态不持久化」等文案随实际行为更新，加「重置进度」小按钮。
2. **进度总览**（dashboard，与持久化同批落地，避免出现恒为 0 的空条）：
   公共必读 x/13、90 天周次 y/12、核验销项 z/12，三条进度条。
3. 不做导出/导入、不做同步（过度设计）。

验收：勾选刷新后保留；禁用 localStorage 时无报错、行为同现状。

---

## 4. 待所有者确认项（不影响阶段 1—3 先行）

- **`firstMail / reportDeck / reportTips` 数据在但页面未渲染。** 建议：在 research 页以折叠块
  「见导师材料（邮件 / 汇报骨架）」恢复渲染（成本低、信息不丢）；若导师已见过、材料已过期，
  则从 data 删除而非隐藏。两选一，实施前定。

## 5. 不做清单（防范围蔓延）

- 不换技术栈、不引框架/库/字体、不增页面、不动导航 10 项与 URL 结构
- 不做页级「远期参考」标签（jobs/skills 含研一执行内容，页级降级会误伤）
- 不做 jobs 默认阶段筛选（匹配仅 2 条，页面近空）
- 不做后端/账号/同步/导出、不做第三主题、不做复杂动画
- 不改证据分级、数据口径、id 命名规则；已达标页面（tools/career/portfolio）不动

## 6. 风险与回退

| 风险 | 缓解 |
|---|---|
| 要点与正文不一致 | 要点从当前句子提炼，实施时逐条核对出处；验收抽查 |
| 又有并行修订改数据 | 实施前先跑一次现状核对（本方案第 0 节表格即基线），不一致先更新方案 |
| 阶段判断硬编码过期 | 里程碑为渲染器顶部常量，注释「每年复核」 |
| 持久化 id 漂移 | 不改 id 命名规则（MAINTENANCE 第 7 节警告） |

回退：四阶段独立，任一可单独 git 回滚。

## 7. 验收流程

1. `node --check` 全部 JS；`node _render_smoke.js` 十路由无异常、无 undefined 泄漏；
2. 浏览器逐页冒烟 + 折叠展开核对；
3. 三个衡量目标（首屏要点 / 情报默认折叠 / 30 秒知道该做什么）逐页人工核对；
4. 三个日期点阶段渲染核对；5. 持久化开/关两态核对（阶段 4）；
6. 同步更新 MAINTENANCE.md（要点块组件、折叠约定、持久化落地）。
